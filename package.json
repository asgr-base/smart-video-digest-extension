{
  "name": "smart-video-digest-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension: summarize YouTube videos with chapters using Chrome's built-in AI (Gemini Nano)",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
